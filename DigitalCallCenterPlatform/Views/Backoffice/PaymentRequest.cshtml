@model IEnumerable<DigitalCallCenterPlatform.Models.PaymentsModels>
@{
    ViewBag.Title = "PaymentRequest";
}

<h2>PaymentRequest</h2>

<hr />

<div style="overflow-y: scroll; overflow-x: hidden; height: 400px;">
    <table class="table table-striped table-bordered">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AccountNumber)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ClientID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ClientReference)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Invoice)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Amount)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PaymentDate)
            </th>
            <th>
                Action/Status
            </th>
        </tr>

        @foreach (var item in Model)
        {
            <tr style="text-align:center;">
                <td class="filterable-cell">
                    @Html.DisplayFor(modelItem => item.AccountNumber)
                </td>
                <td class="filterable-cell">
                    @Html.DisplayFor(modelItem => item.ClientID)
                </td>
                <td class="filterable-cell">
                    @Html.DisplayFor(modelItem => item.ClientReference)
                </td>
                <td class="filterable-cell">
                    @Html.DisplayFor(modelItem => item.Invoice)
                </td>
                <td class="filterable-cell">
                    @Html.DisplayFor(modelItem => item.Amount)
                </td>
                <td class="filterable-cell">
                    @Html.DisplayFor(modelItem => item.PaymentDate)
                </td>
                <td>
                    @if (item.Approve)
                    {
                        if (item.PostedFlag)
                        {
                            <a>Already Posted!</a>
                        }
                        else
                        {
                            @Html.ActionLink("Post", "PaymentPost", new { id = item.Id })
                        }
                    }
                    else
                    {
                        <a>Awaiting Approval!</a>
                    }
                </td>
            </tr>
        }
    </table>
</div>
