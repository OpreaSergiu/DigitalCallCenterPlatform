@model DigitalCallCenterPlatform.Models.NewBusinessViewModel

@{
    ViewBag.Title = "Newbusiness";
}

<h2>Newbusiness</h2>

<div class="row">
    <div class="col-sm-6 text-left">
        <table class="table table-striped table-bordered">
            <thead>
                <tr>
                    <th>Files</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model.Files)
                {
                    var fileName = Path.GetFileName(item);
                    <tr>
                        <td class="filterable-cell" style="word-break: keep-all;">@Html.ActionLink(fileName, "ProcessFile", new { Path = item, Name = fileName })</td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
    <div class="col-sm-6 text-left">
        <br />
        <b>Batch Result:</b>
        <br />
        <div class="well">
            <p>
                File: @ViewBag.filename
                <br />
                --------------------------------------------------
                <br />
                New accounts: @ViewBag.accounts
                <br />
                --------------------------------------------------
                <br />
                New invoices: @ViewBag.invoices
                <br />
                --------------------------------------------------
                <br />
                Total balance: @ViewBag.balance
            </p>
        </div>
        <button type="button" class="btn">Commit Results</button>
        <button type="button" class="btn">Ignore Results</button>
        <br />
        <br />
    </div>
</div>
